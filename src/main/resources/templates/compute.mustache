{{> header}}
<div class="container">
    <div class="row">
        <div class="jumbotron jumbotron-fluid">
            <h1 class="display">Bil481-HW1-Finding Common elements in Array Lists</h1>
            <p class="lead">This tool expects you to enter two arrays, min and max value, then outputs a arraylists of integers which every element on this array exists in arraylist1 and arraylist2 and the value is between min and max value. Also duplicate values will only count 1 times.</p>
            Result is {{result}}

        </div>
        <div class="row col-lg-8 offset-lg-2">
            <form class="" action="compute" method="post">
                <div class="container">
                    <div class="col-lg-6 form-group">
                        <label class="text-center" for="input1">Enter the elements of the ArrayList 1, please enter 1 integer on every line</label>
                        <textarea class="form-control" id="input1" name="input1" rows="10"></textarea>
                    </div>
                    <div class="col-lg-6 form-group">
                        <label class="text-center" for="input2">Enter the elements of the ArrayList 2, please enter 1 integer on every line </label>
                        <textarea class="form-control" id="input2" name="input2" rows="10" ></textarea>
                    </div>
                </div>
                <div class="container">
                    <div class="form-group col-lg-6">
                        <label for="input3">The min value for the search</label>
                        <textarea class="form-control" id="input3" name="input3" rows="1" cols="5"></textarea>
                    </div>
                    <div class="form-group col-lg-6">
                        <label for="input4">The max value for the search</label>
                        <textarea class="form-control" id="input4" name="input4" rows="1" cols="5"></textarea>
                    </div>
                </div>
                <div class=container>
                <div class="text-center">
                        <button type="submit" class="btn btn-primary">Submit</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
{{> footer}}
